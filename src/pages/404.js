import React from "react"

import Layout from "../components/layout"

const NotFoundPage = () =>
  <Layout>
    <h1>Not Found</h1>
    <p>You just hit a route that doesn't exist... the sadness. <span role={"img"} aria-label={"crying cat face"}>😿</span></p>
  </Layout>

export default NotFoundPage